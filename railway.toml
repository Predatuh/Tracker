[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m gunicorn -w 1 --timeout 120 --bind 0.0.0.0:$PORT wsgi:app"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
